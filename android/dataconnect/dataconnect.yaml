specVersion: "v1beta"
serviceId: "android"
location: "us-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "android-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
